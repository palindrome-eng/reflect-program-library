/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { RLP_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CREATE_PERMISSION_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  168, 123, 227, 158, 114, 102, 13, 95,
]);

export function getCreatePermissionAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PERMISSION_ACCOUNT_DISCRIMINATOR,
  );
}

export type CreatePermissionAccountInstruction<
  TProgram extends string = typeof RLP_PROGRAM_ADDRESS,
  TAccountSettings extends string | AccountMeta<string> = string,
  TAccountNewCreds extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSettings extends string
        ? ReadonlyAccount<TAccountSettings>
        : TAccountSettings,
      TAccountNewCreds extends string
        ? WritableAccount<TAccountNewCreds>
        : TAccountNewCreds,
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePermissionAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAdmin: Address;
};

export type CreatePermissionAccountInstructionDataArgs = { newAdmin: Address };

export function getCreatePermissionAccountInstructionDataEncoder(): FixedSizeEncoder<CreatePermissionAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAdmin", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_PERMISSION_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

export function getCreatePermissionAccountInstructionDataDecoder(): FixedSizeDecoder<CreatePermissionAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAdmin", getAddressDecoder()],
  ]);
}

export function getCreatePermissionAccountInstructionDataCodec(): FixedSizeCodec<
  CreatePermissionAccountInstructionDataArgs,
  CreatePermissionAccountInstructionData
> {
  return combineCodec(
    getCreatePermissionAccountInstructionDataEncoder(),
    getCreatePermissionAccountInstructionDataDecoder(),
  );
}

export type CreatePermissionAccountInput<
  TAccountSettings extends string = string,
  TAccountNewCreds extends string = string,
  TAccountCaller extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  settings: Address<TAccountSettings>;
  newCreds: Address<TAccountNewCreds>;
  caller: TransactionSigner<TAccountCaller>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAdmin: CreatePermissionAccountInstructionDataArgs["newAdmin"];
};

export function getCreatePermissionAccountInstruction<
  TAccountSettings extends string,
  TAccountNewCreds extends string,
  TAccountCaller extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RLP_PROGRAM_ADDRESS,
>(
  input: CreatePermissionAccountInput<
    TAccountSettings,
    TAccountNewCreds,
    TAccountCaller,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreatePermissionAccountInstruction<
  TProgramAddress,
  TAccountSettings,
  TAccountNewCreds,
  TAccountCaller,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RLP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    settings: { value: input.settings ?? null, isWritable: false },
    newCreds: { value: input.newCreds ?? null, isWritable: true },
    caller: { value: input.caller ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.settings),
      getAccountMeta(accounts.newCreds),
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePermissionAccountInstructionDataEncoder().encode(
      args as CreatePermissionAccountInstructionDataArgs,
    ),
    programAddress,
  } as CreatePermissionAccountInstruction<
    TProgramAddress,
    TAccountSettings,
    TAccountNewCreds,
    TAccountCaller,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePermissionAccountInstruction<
  TProgram extends string = typeof RLP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    settings: TAccountMetas[0];
    newCreds: TAccountMetas[1];
    caller: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreatePermissionAccountInstructionData;
};

export function parseCreatePermissionAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatePermissionAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      settings: getNextAccount(),
      newCreds: getNextAccount(),
      caller: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePermissionAccountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
