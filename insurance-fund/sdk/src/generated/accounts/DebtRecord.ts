/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link DebtRecord}
 * @category Accounts
 * @category generated
 */
export type DebtRecordArgs = {
  amount: beet.bignum
  asset: web3.PublicKey
  lockup: beet.bignum
  timestamp: beet.bignum
}

export const debtRecordDiscriminator = [95, 54, 225, 17, 213, 171, 2, 190]
/**
 * Holds the data for the {@link DebtRecord} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class DebtRecord implements DebtRecordArgs {
  private constructor(
    readonly amount: beet.bignum,
    readonly asset: web3.PublicKey,
    readonly lockup: beet.bignum,
    readonly timestamp: beet.bignum
  ) {}

  /**
   * Creates a {@link DebtRecord} instance from the provided args.
   */
  static fromArgs(args: DebtRecordArgs) {
    return new DebtRecord(args.amount, args.asset, args.lockup, args.timestamp)
  }

  /**
   * Deserializes the {@link DebtRecord} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [DebtRecord, number] {
    return DebtRecord.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link DebtRecord} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<DebtRecord> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find DebtRecord account at ${address}`)
    }
    return DebtRecord.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'rhLMe6vyM1wVLJaxrWUckVmPxSia58nSWZRDtYQow6D'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, debtRecordBeet)
  }

  /**
   * Deserializes the {@link DebtRecord} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [DebtRecord, number] {
    return debtRecordBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link DebtRecord} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return debtRecordBeet.serialize({
      accountDiscriminator: debtRecordDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link DebtRecord}
   */
  static get byteSize() {
    return debtRecordBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link DebtRecord} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      DebtRecord.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link DebtRecord} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === DebtRecord.byteSize
  }

  /**
   * Returns a readable version of {@link DebtRecord} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      amount: (() => {
        const x = <{ toNumber: () => number }>this.amount
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      asset: this.asset.toBase58(),
      lockup: (() => {
        const x = <{ toNumber: () => number }>this.lockup
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      timestamp: (() => {
        const x = <{ toNumber: () => number }>this.timestamp
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const debtRecordBeet = new beet.BeetStruct<
  DebtRecord,
  DebtRecordArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['amount', beet.u64],
    ['asset', beetSolana.publicKey],
    ['lockup', beet.u64],
    ['timestamp', beet.u64],
  ],
  DebtRecord.fromArgs,
  'DebtRecord'
)
